Sticker Assets (PLACEHOLDER)

These SVGs are placeholders so the decoration system is testable end-to-end. 
Replace them with real PNG/WebP sticker files later -- just drop files into the 
matching category folder (flowers/, stars/, hearts/, stamps/, tape/, teddy/, misc/) 
and register them in src/components/editor/stickerLibrary.js. No other code 
needs to change.